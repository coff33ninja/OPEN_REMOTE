# Shared Plugins

The repository keeps builtin agent plugins under `agent/plugins/` so the Go module can compile cleanly on its own.

This top-level directory is reserved for future shared plugin assets, packaging metadata, or multi-language plugin bundles.
